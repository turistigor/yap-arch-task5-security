apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: secrets-manager
rules:
# Полное управление секретами во всех namespace
- apiGroups: [""]
  resources:
    - "secrets"
  verbs: ["create", "delete", "deletecollection", "get", "list", "patch", "update", "watch"]
